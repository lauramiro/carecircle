export default function CreateGroupPage() {
  return (
    <section>
      <h1
        style={{
          color: 'var(--color-text-primary)',
          fontSize: '26px',
          fontWeight: 800,
          letterSpacing: '-0.03em',
          margin: 0,
        }}
      >
        Create Group
      </h1>
      <p className="mt-2 text-sm" style={{ color: 'var(--color-text-secondary)' }}>
        Group creation will be added in a later step.
      </p>
    </section>
  );
}
